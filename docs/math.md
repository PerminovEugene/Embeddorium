# RAG

## Naive

The basic search teqnique.

## Hynbrid

the idea is using lexical search (for example bm25) and vector search (for example cosine similarity) and then merge results using selected algorythm (for example Reciprocal Rank Fusion).

PG doesn't have true BM25, but has close analogue.
