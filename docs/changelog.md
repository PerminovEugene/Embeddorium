# Changelog

This project has no Git tags or published release notes in the repository. The
package version is currently `0.1.0` in `pyproject.toml`.

## Unreleased

The current codebase includes:

- Web and local-XML ingestion through shared plugin-backed actors.
- Per-run dataset, provider, chunker, and vector-store snapshots.
- Structured parser output and chunk metadata with configured JSON size limits.
- Semantic, PostgreSQL BM25, and hybrid RRF search.
- Optional HTTP cross-encoder reranking for hybrid search.
- Persisted search history and a Search Lab comparison UI.
- Auto-discovered actor, chunker, and provider-type plugins.

## Released versions

{MISSED_INFO}
